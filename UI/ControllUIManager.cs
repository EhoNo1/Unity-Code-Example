using UnityEngine;

public class ControllUIManager : MonoBehaviour
{
    /*private ButtonPrompt[] prompts;

    // Start is called before the first frame update
    void Start()
    {
        prompts = GetComponentsInChildren<ButtonPrompt>();
    }

    public void UpdateButtonPrompts(InputDevice device)
    {
        if (prompts == null) { return; }
        for (int i = 0; i < prompts.Length; i++)
        {
            prompts[i].UpdateButtonPrompts(device);
        }
    }*/
}
